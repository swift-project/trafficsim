1) # sudo apt-get install libasound2-dev
2) ZIP entpacken in /var/www/....
3) # sudo ln -s /var/www/.../lib/libvatlib.so /usr/lib/libvatlib.so

!! UPDATE !!

Since last Update, we need sqlite to run a little database. Install it with:
sudo apt-get install php5-sqlite apache2-mpm-prefork

